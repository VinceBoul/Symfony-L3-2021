{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.content) }}

       <img class="img-fluid" alt="image" src="{{ asset('uploads/articles/' ~ article.image) }}">

{{ form_end(form) }}
